#include "../Release.xcconfig"

// Build/archive settings
MAIN_TARGET_BUNDLE_ID = com.ridgeviewapps.earnings-meter

// App environment / Info.plist values
APP_GROUP_NAME = group.com.ridgeviewapps.earnings-meter.sharedData
WIDGET_OVERLAY_TITLE =
